# ==============================================================================
#  AI Bookkeeping Agent Platform — Python Dependencies
#  Install with:  pip install -r requirements.txt
# ==============================================================================

# --- Web framework ---
fastapi==0.115.12
uvicorn[standard]==0.34.0

# --- Configuration ---
pydantic==2.11.3
pydantic-settings==2.9.1

# --- HTTP client (async) ---
httpx==0.28.1

# --- Database (async) ---
sqlalchemy[asyncio]==2.0.39
asyncpg==0.30.0
alembic==1.15.1

# --- Security ---
python-jose[cryptography]==3.5.0   # JWT encoding / decoding
passlib[bcrypt]==1.7.4             # Password hashing (Phase 2 user auth)

# --- Environment ---
python-dotenv==1.1.0

# --- Type stubs (dev) ---
types-python-jose==3.3.4.20240106
